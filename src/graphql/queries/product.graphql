query getAllProductSlugs {
	allProducts {
		slug
	}
}

query getSpecificProduct($slug: String!) {
	productBySlug(slug: $slug) {
		id
		name
		slug
		new
		price
		description
		image {
			desktop
			altText
			tablet
			mobile
		}
	}
}
